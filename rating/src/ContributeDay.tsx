import { useState } from 'react';
import "./ContributeDay.css";

interface DayProps extends React.PropsWithChildren{
    date: Date
}
export const ContributeDay = (props: DayProps) => {
    return <p className='ContributeDay'>

    </p>
};